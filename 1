#!/bin/bash

# Navigate to RePlAce's directory if it's not in the PATH
cd /build

# Assuming replace is the executable and expects a Tcl script as input
./replace -f /home/openroad/RePlAce/run_placement.tcl

# You can add additional commands here if needed, for example:
# echo "Placement task completed."

